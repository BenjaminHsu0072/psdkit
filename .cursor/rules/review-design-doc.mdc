---
description: 评审方案文档 - 当用户要求审查/评估/Review某个方案文档时使用
alwaysApply: false
---
# 方案文档评审规则

## 触发条件

当用户要求"评审"/"审查"/"Review"/"评估"某个方案文档时，按以下流程执行。

## 工作方式（强约束）

- 默认采用**单轮全量评审**。
- 首轮目标：尽量一次性暴露 95% 关键风险，减少后续轮次新增问题。
- 纯风格偏好（不影响契约、可执行性、稳定性）不得列为阻塞问题。
- 基线未锁定的实现细节不得强行升格为契约问题。
- 每个问题必须包含：**现状证据 + 基线依据 + 最小修复建议**。
- 若用户明确要求多轮增量评审，则优先遵从用户指令。

## 评审流程

### 1) 读取并理解方案文档

- 完整阅读被评审文档，提取目标、范围、边界、依赖。
- 识别文档涉及的代码模块、API、数据结构、测试口径。

### 2) 对照代码与基线事实

- 根据文档中提到的类型/API/路径，**实际查阅对应代码或文档事实**。
- 不能仅凭方案文字推断；关键假设必须有可追溯证据。
- 对照范围至少包含：
  - `[LOCKED]` 基线文档（需求/API/数据结构/开发计划）
  - 决策清单
  - 变量与常量约定
  - 配置契约清单（如适用）

### 3) 七维全检（首轮必须逐项覆盖）

1. **契约一致性**：是否与基线/决策冲突  
2. **可达性**：目标分支是否会被前置校验提前拦截  
3. **覆盖闭环**：正负路径、边界对偶、关键分支是否闭环  
4. **断言唯一性**：预期是否唯一且可机判  
5. **副作用与状态机**：写库/不写库、上游调用、状态迁移是否可验证  
6. **跨阶段承接**：前向假设是否兑现、是否存在阶段断点  
7. **稳定性**：NOW/TTL 抖动、白盒断言可观测性、偶发失败风险  

### 4) 问题分级与收敛

- 严重度定义：
  - **Critical**：直接违反 LOCKED 核心契约，或会导向错误实现方向
  - **High**：阻塞落地执行（不可达、不可判定、关键路径缺失）
  - **Medium**：影响质量/稳定性，但不阻塞本轮落地
  - **Low**：优化项与可维护性改进
- 必须输出“最小可冻结修复包”（最多 5 项，按优先级）。

### 5) 输出 Review 文档

在**原文档所在的同一文件夹**中生成 Review 文档。若同时评审多份文档，可合并为一份。

#### 命名规则

- 命名统一使用下划线 `_` 分隔，不使用空格
- 首个版本必须显式标注 `V1`
- 默认命名：`_Review_V1`，后续版本：`_Review_V2`、`_Review_V3`
- 若模型为 OpenAI，使用 `_Review_ByCoCo_V1`，后续版本：`_Review_ByCoCo_V2`、`_Review_ByCoCo_V3`
- 若模型为 Anthropic，使用 `_Review_ByAndy_V1`，后续版本：`_Review_ByAndy_V2`、`_Review_ByAndy_V3`
- 示例（默认）：`2026-02-13_纯AST_Undo设计方案.md` → `2026-02-13_纯AST_Undo设计方案_Review_V1.md`
- 示例（OpenAI）：`2026-02-13_纯AST_Undo设计方案.md` → `2026-02-13_纯AST_Undo设计方案_Review_ByCoCo_V1.md`
- 示例（Anthropic）：`2026-02-13_纯AST_Undo设计方案.md` → `2026-02-13_纯AST_Undo设计方案_Review_ByAndy_V1.md`

#### 输出结构（统一模板）

```markdown
# Review：{方案标题}

> Review 日期：{YYYY-MM-DD}
> 被评审文档：`{原文档文件名}`

---

## 结论（TL;DR）

- **总体评价**：一句话结论
- **是否建议冻结**：建议 / 不建议
- **最小可冻结修复包**：最多 5 项（若无写“无”）

---

## 七维覆盖表

| 维度 | 结论（✅/⚠️） | 证据摘要 |
|---|---|---|
| 契约一致性 |  |  |
| 可达性 |  |  |
| 覆盖闭环 |  |  |
| 断言唯一性 |  |  |
| 副作用与状态机 |  |  |
| 跨阶段承接 |  |  |
| 稳定性 |  |  |

---

## 阻塞问题（Critical / High）

### [Sx-xx][严重度][维度] 标题
- 现状：
- 问题：
- 依据：
- 建议（最小修复）：

## 非阻塞问题（Medium / Low）

### [Sx-xx][严重度][维度] 标题
- 现状：
- 问题：
- 依据：
- 建议：

---

## 补充说明

- 本次评审边界：
- 不纳入本轮的问题：
```

## 注意事项

- **引用事实**：指出问题必须附上可追溯依据（代码片段、文档条目、接口定义）。
- **建议可执行**：每个问题都要给出可落地的最小修复建议。
- **控制范围**：只评审当前方案范围，不引入无关优化建议。
- **实事求是**：没有问题不硬找，有问题不回避。
- **避免复读**：后续轮次若文档未改动，不重复提出同一问题（除非有新证据或触发新增准入条件）。
